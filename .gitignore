# ============ Python ============
# 缓存与编译产物
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# 虚拟环境
.venv/
venv/
env/

# 类型检查/测试/缓存
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# ============ 环境变量与密钥 ============
.env
.env.local
*.env
*.key
*.secret

# ============ Docker ============
# 容器运行时产生的数据卷(本地开发时可能用 bind mount 生成)
mysql_data/
milvus_data/
etcd_data/
minio_data/
ollama_data/

# ============ 模型与向量缓存 ============
# Ollama 模型文件巨大(几 GB),坚决不上传
*.gguf
ollama_models/
# 本地向量/分块缓存
vector_cache/
chunk_cache/
*.pkl
faiss_index/

# ============ 日志与临时文件 ============
logs/
*.log
*.tmp
*.bak

# ============ IDE ============
.idea/
.vscode/

# ============ 系统文件 ============
.DS_Store
Thumbs.db

# ============ 说明 ============
# 注意: data/documents/ 下的公开法律法规 docx 不忽略,随仓库分发以便他人 clone 后可直接运行项目
# 注意: init.sql 是建表脚本,需上传;若后续有含真实案例数据的 init_data.sql 则不应上传
