[project]
name = "legal-chatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tangfy", email = "1303765316@qq.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
legal-chatbot = "legal_chatbot:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
